[project]
name = "analytic-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.133.1",
    "uvicorn",
    "gunicorn",
    "timescaledb",
    "sqlmodel",
    "pydantic",
    "sqlalchemy"
]
